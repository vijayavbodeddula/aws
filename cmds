                                            session-59
                                            ---------
                                            Ebs-mounting-and-testing
cmds
----
1) sudo lsblk
2) sudo df -kh
3) sudo mkfs.ext4 /dev/xvdf
4) sudo mount /dev/xvdf /var/lib/jenkins
5) cat /etc/fstab
6) sudo vi /etc/fstab   (ake an entry for newly mounted dev using UUID)
7) sudo umount /var/lib/jenkins
8) sudo mount -a (as per the entries in the fstab files this cmd will 
                  remount the path with devices then we can confirm it works fine)